============
+ ray->pos.x × ray->pos.x + ray->pos.y × ray->pos.y - A × A × ray->pos.z × ray->pos.z ^ 1
============
+ t × (2.0 × ray->pos.x × ray->dir.x + 2.0 × ray->pos.y × ray->dir.y - 2.0 × A × A × ray->pos.z × ray->dir.z)
===============
+ t × t × (ray->dir.x × (ray->dir.x + ray->dir.y × ray->dir.y - A × A × ray->dir.z × ray->dir.z)
